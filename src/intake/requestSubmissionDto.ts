/**
 * Compatibility shim — intake DTO rules live in `src/features/intake/domain`.
 * Prefer importing from `src/features/intake` for new code.
 */
export * from '../features/intake/domain/requestSubmissionDto';
