/** Public HTTP contract constants for intake (preserve verbatim). */
export const PUBLIC_INTAKE_SUCCESS_MESSAGE =
  'Form data received, onto processing';

export const PUBLIC_INTAKE_PATH = '/requestService/requestSubmission';
